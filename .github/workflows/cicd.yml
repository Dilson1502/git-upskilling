on:
  push:
name: action
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Step Inicial
        id: hello
        uses: dilson1502/docker-action
        with:
          who-to-great: "Dilson"
      - name: Step Final
        run: echo "La fecha fue ${{ steps.hello.outputs.time}}"
